# Godot 4+ specific ignores
.godot/

# --- Godot 4 核心忽略 ---
.godot/                 # 核心缓存目录（资源导入、着色器编译等）
/android/
*.tmp                   # 临时文件
*.import                # 可选：如果希望其他人在打开项目时重新生成导入，可忽略（通常建议保留）
export_presets.cfg      # 包含发布秘钥等敏感信息（可选，建议忽略）

# --- IDE 相关 (Rider / Visual Studio / VS Code) ---
.idea/                  # Rider 配置
.vs/                    # Visual Studio 配置
*.sln                   # 解决方案文件（可由 Godot 生成）
*.csproj                # 项目文件（可由 Godot 生成）
*.DotSettings.user      # Rider 用户个人设置
.vscode/                # VS Code 配置

# --- C# / .NET 编译产物 ---
bin/                    # 编译后的二进制文件
obj/                    # 编译中间文件
.mono/                  # 旧版 Godot/Mono 缓存

# --- 操作系统临时文件 ---
.DS_Store               # macOS 
Thumbs.db               # Windows
desktop.ini             # Windows

# --- 导出结果 (根据你的导出习惯) ---
build/                  # 建议将所有导出文件统一放在此目录下
exports/                # 另一个常用的导出目录名